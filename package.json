{
  "name": "restaurant-pos-front",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean && rm -rf node_modules",
    "generate:types": "openapi-typescript ../api/swagger/v1/swagger.json -o types/api.ts"
  },
  "devDependencies": {
    "openapi-typescript": "^7.4.4",
    "turbo": "^2.3.0"
  },
  "packageManager": "pnpm@9.15.1",
  "engines": {
    "node": ">=18"
  }
}
